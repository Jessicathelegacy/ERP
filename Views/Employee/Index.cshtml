@model IEnumerable<Employee>
@{
    ViewData["Title"] = "Employees";
    bool hasFilter = !string.IsNullOrWhiteSpace(ViewBag.Search as string)
                  || (ViewBag.SchemeId as int?) != null
                  || !string.IsNullOrWhiteSpace(ViewBag.Status as string);
}

@if (TempData["Success"] != null)
{
    <div class="alert alert-success alert-dismissible fade show mb-3" role="alert">
        <i class="bi bi-check-circle-fill me-2"></i>@TempData["Success"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="card">
    <div class="card-header d-flex align-items-center justify-content-between">
        <span><i class="bi bi-people-fill me-2 text-primary"></i>Employees</span>
        <a asp-action="Create" class="btn btn-primary btn-sm px-3">
            <i class="bi bi-plus-lg me-1"></i>Add New
        </a>
    </div>

    @* Filter bar *@
    <div class="card-body border-bottom pb-3">
        <form method="get" asp-action="Index" class="row g-2 align-items-end">
            <div class="col-12 col-sm-6 col-md-4">
                <label class="form-label small fw-semibold mb-1">Search</label>
                <div class="input-group input-group-sm">
                    <span class="input-group-text"><i class="bi bi-search"></i></span>
                    <input type="text" name="search" value="@ViewBag.Search" class="form-control" placeholder="Name or email…" />
                </div>
            </div>
            <div class="col-6 col-sm-4 col-md-3">
                <label class="form-label small fw-semibold mb-1">Payment Scheme</label>
                <select name="schemeId" class="form-select form-select-sm">
                    <option value="">All Schemes</option>
                    @foreach (SelectListItem item in ViewBag.Schemes)
                    {
                        <option value="@item.Value" selected="@(item.Selected ? "selected" : null)">@item.Text</option>
                    }
                </select>
            </div>
            <div class="col-6 col-sm-4 col-md-2">
                <label class="form-label small fw-semibold mb-1">Status</label>
                <select name="status" class="form-select form-select-sm">
                    <option value="">All Statuses</option>
                    <option value="active"   selected="@(ViewBag.Status == "active"   ? "selected" : null)">Active</option>
                    <option value="inactive" selected="@(ViewBag.Status == "inactive" ? "selected" : null)">Inactive</option>
                </select>
            </div>
            <div class="col-12 col-sm-auto d-flex gap-2">
                <button type="submit" class="btn btn-sm btn-primary px-3">
                    <i class="bi bi-funnel-fill me-1"></i>Filter
                </button>
                @if (hasFilter)
                {
                    <a asp-action="Index" class="btn btn-sm btn-outline-secondary px-3">
                        <i class="bi bi-x-lg me-1"></i>Clear
                    </a>
                }
            </div>
        </form>
    </div>

    <div class="card-body p-0">
        @if (Model.Any())
        {
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-light">
                        <tr>
                            <th class="ps-4" style="width:40px">#</th>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Phone</th>
                            <th>Payment Scheme</th>
                            <th class="text-center">Join Date</th>
                            <th class="text-center">Status</th>
                            <th class="text-center pe-4" style="width:80px">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{ int row = (ViewBag.Page - 1) * ViewBag.PageSize + 1; }
                        @foreach (var employee in Model)
                        {
                            <tr>
                                <td class="ps-4 text-muted small">@row</td>
                                <td class="fw-semibold">@employee.Name</td>
                                <td class="text-muted small">@employee.Email</td>
                                <td class="text-muted small">@(employee.Phone ?? "—")</td>
                                <td>
                                    <span class="badge rounded-pill bg-primary-subtle text-primary px-3 py-2">
                                        @employee.PaymentScheme.Name
                                    </span>
                                </td>
                                <td class="text-center small">@employee.JoinDate.ToString("dd MMM yyyy")</td>
                                <td class="text-center">
                                    @if (employee.IsActive)
                                    {
                                        <span class="badge rounded-pill bg-success-subtle text-success px-3 py-2">
                                            <i class="bi bi-check-circle-fill me-1"></i>Active
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="badge rounded-pill bg-secondary-subtle text-secondary px-3 py-2">
                                            <i class="bi bi-x-circle-fill me-1"></i>Inactive
                                        </span>
                                    }
                                </td>
                                <td class="text-center pe-4">
                                    <a asp-action="Edit" asp-route-id="@employee.Id" class="btn btn-sm btn-outline-primary" title="Edit">
                                        <i class="bi bi-pencil-fill"></i>
                                    </a>
                                </td>
                            </tr>
                            row++;
                        }
                    </tbody>
                </table>
            </div>
            @await Html.PartialAsync("_Pagination")
        }
        else
        {
            <div class="text-center py-5">
                <div class="empty-state-icon mx-auto mb-3">
                    <i class="bi bi-people"></i>
                </div>
                @if (hasFilter)
                {
                    <h6 class="fw-semibold text-dark mb-1">No matching employees</h6>
                    <p class="text-muted small mb-4">Try adjusting your filters.</p>
                    <a asp-action="Index" class="btn btn-outline-secondary px-4">
                        <i class="bi bi-x-lg me-1"></i>Clear Filters
                    </a>
                }
                else
                {
                    <h6 class="fw-semibold text-dark mb-1">No employees yet</h6>
                    <p class="text-muted small mb-4">Add your first employee to get started.</p>
                    <a asp-action="Create" class="btn btn-primary px-4">
                        <i class="bi bi-plus-lg me-1"></i>Add Employee
                    </a>
                }
            </div>
        }
    </div>
</div>
