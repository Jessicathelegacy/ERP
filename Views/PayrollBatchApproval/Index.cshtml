@model IEnumerable<PayrollBatch>
@{
    ViewData["Title"] = "Batch Approval";
}

@if (TempData["Success"] != null)
{
    <div class="alert alert-success alert-dismissible fade show mb-3" role="alert">
        <i class="bi bi-check-circle-fill me-2"></i>@TempData["Success"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}
@if (TempData["Info"] != null)
{
    <div class="alert alert-info alert-dismissible fade show mb-3" role="alert">
        <i class="bi bi-info-circle-fill me-2"></i>@TempData["Info"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show mb-3" role="alert">
        <i class="bi bi-exclamation-triangle-fill me-2"></i>@TempData["Error"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="card">
    <div class="card-header d-flex align-items-center justify-content-between">
        <span><i class="bi bi-patch-check-fill me-2 text-warning"></i>Pending Approvals</span>
        <span class="badge bg-warning text-dark rounded-pill px-3">@Model.Count() awaiting review</span>
    </div>

    @* Filter bar *@
    <div class="card-body border-bottom pb-3">
        <form method="get" asp-action="Index" class="row g-2 align-items-end">
            <div class="col-6 col-sm-3 col-md-2">
                <label class="form-label small fw-semibold mb-1">Year</label>
                <select name="year" class="form-select form-select-sm">
                    <option value="">All Years</option>
                    @foreach (int y in ViewBag.Years)
                    {
                        <option value="@y" selected="@((ViewBag.Year as int?) == y ? "selected" : null)">@y</option>
                    }
                </select>
            </div>
            <div class="col-6 col-sm-3 col-md-2">
                <label class="form-label small fw-semibold mb-1">Month</label>
                <select name="month" class="form-select form-select-sm">
                    <option value="">All Months</option>
                    @for (int m = 1; m <= 12; m++)
                    {
                        <option value="@m" selected="@((ViewBag.Month as int?) == m ? "selected" : null)">
                            @(new DateTime(2000, m, 1).ToString("MMMM"))
                        </option>
                    }
                </select>
            </div>
            <div class="col-6 col-sm-auto d-flex gap-2">
                <button type="submit" class="btn btn-sm btn-primary px-3">
                    <i class="bi bi-funnel-fill me-1"></i>Filter
                </button>
                @if ((ViewBag.Year as int?) != null || (ViewBag.Month as int?) != null)
                {
                    <a asp-action="Index" class="btn btn-sm btn-outline-secondary px-3">
                        <i class="bi bi-x-lg me-1"></i>Clear
                    </a>
                }
            </div>
        </form>
    </div>

    <div class="card-body p-0">
        @if (Model.Any())
        {
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-light">
                        <tr>
                            <th class="ps-4" style="width:40px">#</th>
                            <th>Period</th>
                            <th>Description</th>
                            <th class="text-center">Employees</th>
                            <th class="text-end">Total Net</th>
                            <th>Submitted By</th>
                            <th class="text-center">Submitted</th>
                            <th class="text-center pe-4" style="width:100px">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{ int row = (ViewBag.Page - 1) * ViewBag.PageSize + 1; }
                        @foreach (var b in Model)
                        {
                            <tr>
                                <td class="ps-4 text-muted small">@row</td>
                                <td class="fw-semibold">@(new DateTime(b.Year, b.Month, 1).ToString("MMMM yyyy"))</td>
                                <td class="text-muted small">@(b.Description ?? "—")</td>
                                <td class="text-center">
                                    <span class="badge bg-primary-subtle text-primary rounded-pill px-3 py-2">
                                        @b.TotalEmployees
                                    </span>
                                </td>
                                <td class="text-end fw-semibold">@b.TotalNetSalary.ToString("N2")</td>
                                <td class="small">
                                    <i class="bi bi-person-fill me-1 text-muted"></i>
                                    @(b.SubmittedByAdminUser?.Username ?? "—")
                                </td>
                                <td class="text-center small text-muted">
                                    @(b.SubmittedAt.HasValue ? b.SubmittedAt.Value.ToString("dd MMM yyyy") : "—")
                                </td>
                                <td class="text-center pe-4">
                                    <a asp-action="Review" asp-route-id="@b.Id" class="btn btn-sm btn-warning px-3">
                                        <i class="bi bi-eye-fill me-1"></i>Review
                                    </a>
                                </td>
                            </tr>
                            row++;
                        }
                    </tbody>
                </table>
            </div>
            @await Html.PartialAsync("_Pagination")
        }
        else
        {
            <div class="text-center py-5">
                <div class="empty-state-icon mx-auto mb-3">
                    <i class="bi bi-patch-check"></i>
                </div>
                <h6 class="fw-semibold text-dark mb-1">No batches pending approval</h6>
                <p class="text-muted small mb-0">Submitted payroll batches awaiting your review will appear here.</p>
            </div>
        }
    </div>
</div>
